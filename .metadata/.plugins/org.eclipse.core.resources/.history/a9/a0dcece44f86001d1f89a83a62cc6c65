package javaclass.ex01;

public class ClassBasic {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		// 객체
		/*
		 * 물리적으로 존재하거나 추상적으로 생각할 수 있는 것 중에서 자신의 속성을 가지며 식별가능한 것
		 * 속성(필드) + 동작(메소드)으로 구성
		 * e.g. 현실의 사람: 속성 = 이름, 나이 .. 동작 = 웃다, 먹다 ..
		 * >> 객체로 치환하면 사람 객체의 이름, 나이 .. 가 필드 웃다, 먹다 .. 가 메소드
		 */
		
		// 객체와 객체 간 상호작용
		/*
		 * 메소드를 통해 객체들이 상호작용
		 */
		
		// 객체 간의 관계
		/*
		 * 1. 집합 관계
		 * - 부품과 완성품의 관계 (엔진, 타이어, 핸들은 자동차의 집합)
		 * 
		 * 2. 사용 관계
		 * - 객체간 상호작용 (사람 객체가 자동차 객체를 이용한다)
		 * 
		 * 3. 상속 관계
		 * - 상휘(부모) 객체를 기반으로 하위(자식) 객체를 생성
		 * - 기계 객체를 기반으로 자동차 객체를 생성
		 */
		
		//객체 지향 프로그래밍
		/*
		 * 집합, 사용 관계에 있는 객체를 하나씩 설계한 후 조립하여 프로그램 개발
		 */
	}

}
